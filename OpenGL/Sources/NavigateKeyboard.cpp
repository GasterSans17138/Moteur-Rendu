#include "NavigateKeyboard.h"

void GUIitem::Navigate(int _key)
{
	/*if (glfwGetKey(window, GLFW_KEY_RIGHT) == GLFW_PRESS)
	{
		_key += 1;
	}
	if (glfwGetKey(window, GLFW_KEY_LEFT) == GLFW_PRESS)
	{
		_key -= 1;
	}*/
}